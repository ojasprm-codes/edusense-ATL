/* ============================================================
   EDUSENSE AI — Premium IoT Dashboard Stylesheet
   ============================================================ */

:root {
  --bg-primary: #0B1220;
  --card-bg: rgba(255, 255, 255, 0.08);
  --card-border: rgba(255, 255, 255, 0.12);
  --card-hover: rgba(255, 255, 255, 0.12);
  --primary: #00E5FF;
  --secondary: #0099FF;
  --success: #00E676;
  --warning: #FFD54F;
  --danger: #FF5252;
  --text-primary: #FFFFFF;
  --text-secondary: rgba(255, 255, 255, 0.65);
  --text-muted: rgba(255, 255, 255, 0.4);
  --glass-blur: 20px;
  --radius-sm: 8px;
  --radius-md: 14px;
  --radius-lg: 20px;
  --shadow-glow: 0 0 30px rgba(0, 229, 255, 0.15);
  --shadow-card: 0 8px 32px rgba(0, 0, 0, 0.4);
  --transition: 0.35s cubic-bezier(0.4, 0, 0.2, 1);
  --font: 'Poppins', -apple-system, BlinkMacSystemFont, sans-serif;
}

*, *::before, *::after {
  box-sizing: border-box;
  margin: 0;
  padding: 0;
}

html {
  font-size: 16px;
  scroll-behavior: smooth;
  -webkit-tap-highlight-color: transparent;
  width: 100%;
  max-width: 100%;
  overflow-x: hidden;
}

body {
  font-family: var(--font);
  background: var(--bg-primary);
  color: var(--text-primary);
  min-height: 100vh;
  width: 100%;
  max-width: 100%;
  overflow-x: hidden;
  line-height: 1.5;
}

/* ---- Animated Background ---- */
.bg-gradient {
  position: fixed;
  inset: 0;
  z-index: 0;
  background: linear-gradient(
    135deg,
    #0B1220 0%,
    #0D1B2E 25%,
    #0A1628 50%,
    #0C1A30 75%,
    #0B1220 100%
  );
  background-size: 400% 400%;
  animation: gradientShift 15s ease infinite;
}

.bg-grid {
  position: fixed;
  inset: 0;
  z-index: 0;
  background-image:
    linear-gradient(rgba(0, 229, 255, 0.03) 1px, transparent 1px),
    linear-gradient(90deg, rgba(0, 229, 255, 0.03) 1px, transparent 1px);
  background-size: 60px 60px;
  mask-image: radial-gradient(ellipse at center, black 30%, transparent 80%);
}

.bg-glow {
  position: fixed;
  width: 600px;
  height: 600px;
  border-radius: 50%;
  filter: blur(120px);
  opacity: 0.12;
  z-index: 0;
  pointer-events: none;
}

.bg-glow--left {
  top: -200px;
  left: -200px;
  background: var(--primary);
  animation: floatGlow 12s ease-in-out infinite;
}

.bg-glow--right {
  bottom: -200px;
  right: -200px;
  background: var(--secondary);
  animation: floatGlow 12s ease-in-out infinite reverse;
}

@keyframes gradientShift {
  0%, 100% { background-position: 0% 50%; }
  50% { background-position: 100% 50%; }
}

@keyframes floatGlow {
  0%, 100% { transform: translate(0, 0); }
  50% { transform: translate(40px, 30px); }
}

/* ---- Layout ---- */
.app {
  position: relative;
  z-index: 1;
  max-width: 1600px;
  width: 100%;
  min-width: 0;
  margin: 0 auto;
  padding: 16px;
  display: flex;
  flex-direction: column;
  gap: 16px;
  min-height: 100vh;
}

/* ---- Glass Panel ---- */
.glass-panel {
  background: var(--card-bg);
  backdrop-filter: blur(var(--glass-blur));
  -webkit-backdrop-filter: blur(var(--glass-blur));
  border: 1px solid var(--card-border);
  border-radius: var(--radius-lg);
  box-shadow: var(--shadow-card);
}

/* ---- Header ---- */
.header {
  display: flex;
  align-items: center;
  justify-content: space-between;
  padding: 20px 28px;
  gap: 16px;
  min-width: 0;
}

.header__brand {
  display: flex;
  align-items: center;
  gap: 16px;
  min-width: 0;
}

.header__text {
  min-width: 0;
}

.header__logo {
  width: 52px;
  height: 52px;
  flex-shrink: 0;
  animation: logoPulse 4s ease-in-out infinite;
}

.header__logo svg {
  width: 100%;
  height: 100%;
}

@keyframes logoPulse {
  0%, 100% { filter: drop-shadow(0 0 8px rgba(0, 229, 255, 0.4)); }
  50% { filter: drop-shadow(0 0 20px rgba(0, 229, 255, 0.7)); }
}

.header__title {
  font-size: 1.75rem;
  font-weight: 800;
  letter-spacing: 2px;
  line-height: 1.1;
}

.header__title .accent {
  background: linear-gradient(135deg, var(--primary), var(--secondary));
  -webkit-background-clip: text;
  -webkit-text-fill-color: transparent;
  background-clip: text;
}

.header__subtitle {
  font-size: 0.8rem;
  font-weight: 400;
  color: var(--text-secondary);
  letter-spacing: 0.5px;
  margin-top: 2px;
}

.header__clock {
  text-align: right;
  flex-shrink: 0;
}

.header__time {
  display: block;
  font-size: 2rem;
  font-weight: 700;
  font-variant-numeric: tabular-nums;
  letter-spacing: 2px;
  background: linear-gradient(135deg, var(--primary), #FFFFFF);
  -webkit-background-clip: text;
  -webkit-text-fill-color: transparent;
  background-clip: text;
  line-height: 1.1;
}

.header__date {
  display: block;
  font-size: 0.85rem;
  color: var(--text-secondary);
  margin-top: 4px;
  font-weight: 400;
}

/* ---- Status Bar ---- */
.status-bar {
  display: flex;
  align-items: center;
  justify-content: space-around;
  padding: 16px 24px;
  flex-wrap: wrap;
  gap: 12px;
  min-width: 0;
}

.status-bar__item {
  display: flex;
  flex-direction: column;
  align-items: center;
  gap: 6px;
  min-width: 120px;
}

.status-bar__label {
  font-size: 0.7rem;
  font-weight: 500;
  text-transform: uppercase;
  letter-spacing: 1.5px;
  color: var(--text-muted);
}

.status-bar__value {
  font-size: 0.95rem;
  font-weight: 600;
  font-variant-numeric: tabular-nums;
  color: var(--primary);
}

.status-bar__divider {
  width: 1px;
  height: 40px;
  background: var(--card-border);
}

.status-badge {
  display: inline-flex;
  align-items: center;
  padding: 6px 20px;
  border-radius: 50px;
  font-size: 0.85rem;
  font-weight: 700;
  letter-spacing: 2px;
  text-transform: uppercase;
}

.status-badge--safe {
  background: rgba(0, 230, 118, 0.15);
  color: var(--success);
  border: 1px solid rgba(0, 230, 118, 0.3);
  box-shadow: 0 0 20px rgba(0, 230, 118, 0.2);
  animation: statusPulseSafe 3s ease-in-out infinite;
}

.status-badge--calibrating {
  background: rgba(0, 229, 255, 0.15);
  color: var(--primary);
  border: 1px solid rgba(0, 229, 255, 0.3);
  box-shadow: 0 0 20px rgba(0, 229, 255, 0.2);
  animation: statusPulseCalibrating 1.8s ease-in-out infinite;
}

.status-badge--elevated {
  background: rgba(255, 213, 79, 0.11);
  color: #FFE082;
  border: 1px solid rgba(255, 213, 79, 0.22);
  box-shadow: 0 0 20px rgba(255, 213, 79, 0.14);
  animation: statusPulseWarning 2.5s ease-in-out infinite;
}

.status-badge--warning {
  background: rgba(255, 213, 79, 0.15);
  color: var(--warning);
  border: 1px solid rgba(255, 213, 79, 0.3);
  box-shadow: 0 0 20px rgba(255, 213, 79, 0.2);
  animation: statusPulseWarning 2s ease-in-out infinite;
}

.status-badge--danger {
  background: rgba(255, 82, 82, 0.15);
  color: var(--danger);
  border: 1px solid rgba(255, 82, 82, 0.3);
  box-shadow: 0 0 20px rgba(255, 82, 82, 0.3);
  animation: statusPulseDanger 1.2s ease-in-out infinite;
}

@keyframes statusPulseSafe {
  0%, 100% { box-shadow: 0 0 15px rgba(0, 230, 118, 0.15); }
  50% { box-shadow: 0 0 25px rgba(0, 230, 118, 0.35); }
}

@keyframes statusPulseCalibrating {
  0%, 100% { box-shadow: 0 0 15px rgba(0, 229, 255, 0.16); }
  50% { box-shadow: 0 0 30px rgba(0, 229, 255, 0.38); }
}

@keyframes statusPulseWarning {
  0%, 100% { box-shadow: 0 0 15px rgba(255, 213, 79, 0.2); }
  50% { box-shadow: 0 0 30px rgba(255, 213, 79, 0.45); }
}

@keyframes statusPulseDanger {
  0%, 100% { box-shadow: 0 0 15px rgba(255, 82, 82, 0.3); }
  50% { box-shadow: 0 0 35px rgba(255, 82, 82, 0.6); }
}

.connection-dot {
  display: inline-flex;
  align-items: center;
  gap: 8px;
  font-size: 0.85rem;
  font-weight: 600;
  position: relative;
}

.connection-dot__pulse {
  width: 8px;
  height: 8px;
  border-radius: 50%;
  position: relative;
}

.connection-dot--online {
  color: var(--success);
}

.connection-dot--online .connection-dot__pulse {
  background: var(--success);
  box-shadow: 0 0 8px var(--success);
  animation: dotPulse 2s ease-in-out infinite;
}

.connection-dot--offline {
  color: var(--danger);
}

.connection-dot--offline .connection-dot__pulse {
  background: var(--danger);
  box-shadow: 0 0 8px var(--danger);
}

@keyframes dotPulse {
  0%, 100% { transform: scale(1); opacity: 1; }
  50% { transform: scale(1.4); opacity: 0.7; }
}

/* ---- Sensor Grid ---- */
.sensor-grid {
  display: grid;
  grid-template-columns: repeat(4, minmax(0, 1fr));
  gap: 14px;
  min-width: 0;
}

.sensor-card {
  padding: 20px;
  position: relative;
  overflow: hidden;
  transition: transform var(--transition), box-shadow var(--transition), border-color var(--transition);
  cursor: default;
}

.sensor-card::before {
  content: '';
  position: absolute;
  top: 0;
  left: 0;
  right: 0;
  height: 2px;
  background: linear-gradient(90deg, transparent, var(--primary), transparent);
  opacity: 0;
  transition: opacity var(--transition);
}

.sensor-card:hover {
  transform: translateY(-6px);
  box-shadow: var(--shadow-glow), var(--shadow-card);
  border-color: rgba(0, 229, 255, 0.25);
}

.sensor-card:hover::before {
  opacity: 1;
}

.sensor-card--chart-focus {
  border-color: rgba(0, 229, 255, 0.42);
  box-shadow: var(--shadow-glow), var(--shadow-card);
}

.sensor-card--chart-focus::before {
  opacity: 1;
}

.sensor-card__icon {
  width: 40px;
  height: 40px;
  border-radius: var(--radius-sm);
  display: flex;
  align-items: center;
  justify-content: center;
  margin-bottom: 12px;
}

.sensor-card__icon svg {
  width: 22px;
  height: 22px;
}

.sensor-card__icon--temp {
  background: rgba(255, 82, 82, 0.12);
  color: #FF8A80;
}

.sensor-card__icon--hum {
  background: rgba(0, 153, 255, 0.12);
  color: var(--secondary);
}

.sensor-card__icon--gas {
  background: rgba(0, 229, 255, 0.1);
  color: var(--primary);
}

.sensor-card__name {
  font-size: 0.75rem;
  font-weight: 500;
  color: var(--text-secondary);
  text-transform: uppercase;
  letter-spacing: 1px;
  margin-bottom: 8px;
}

.sensor-card__value-wrap {
  display: flex;
  align-items: baseline;
  gap: 4px;
  margin-bottom: 12px;
}

.sensor-card__value {
  font-size: 2rem;
  font-weight: 700;
  font-variant-numeric: tabular-nums;
  line-height: 1;
  background: linear-gradient(180deg, #FFFFFF 0%, rgba(255, 255, 255, 0.7) 100%);
  -webkit-background-clip: text;
  -webkit-text-fill-color: transparent;
  background-clip: text;
}

.sensor-card__unit {
  font-size: 0.85rem;
  font-weight: 500;
  color: var(--text-muted);
}

.sensor-card__status {
  display: flex;
  align-items: center;
  gap: 8px;
}

.status-indicator {
  width: 8px;
  height: 8px;
  border-radius: 50%;
  flex-shrink: 0;
}

.status-indicator--safe {
  background: var(--success);
  box-shadow: 0 0 8px rgba(0, 230, 118, 0.5);
}

.status-indicator--calibrating {
  background: var(--primary);
  box-shadow: 0 0 8px rgba(0, 229, 255, 0.5);
  animation: dotPulse 1.8s ease-in-out infinite;
}

.status-indicator--elevated {
  background: #FFE082;
  box-shadow: 0 0 8px rgba(255, 224, 130, 0.45);
  animation: dotPulse 1.8s ease-in-out infinite;
}

.status-indicator--warning {
  background: var(--warning);
  box-shadow: 0 0 8px rgba(255, 213, 79, 0.5);
  animation: dotPulse 1.5s ease-in-out infinite;
}

.status-indicator--danger {
  background: var(--danger);
  box-shadow: 0 0 8px rgba(255, 82, 82, 0.5);
  animation: dotPulse 1s ease-in-out infinite;
}

.sensor-card__status-text {
  font-size: 0.75rem;
  font-weight: 500;
  color: var(--text-secondary);
}

/* ---- Main Panels ---- */
.main-panels {
  display: grid;
  grid-template-columns: minmax(0, 1fr) minmax(300px, 380px);
  gap: 16px;
  flex: 1;
  min-width: 0;
}

.panel-header {
  display: flex;
  align-items: center;
  justify-content: space-between;
  margin-bottom: 16px;
  flex-wrap: wrap;
  gap: 8px;
}

.panel-header__title {
  display: flex;
  align-items: center;
  gap: 10px;
  font-size: 1rem;
  font-weight: 600;
  letter-spacing: 0.5px;
}

.panel-header__icon {
  width: 20px;
  height: 20px;
  color: var(--primary);
}

/* ---- Chart Panel ---- */
.chart-panel {
  padding: 24px;
  display: flex;
  flex-direction: column;
}

.chart-legend {
  display: flex;
  gap: 16px;
  flex-wrap: wrap;
}

.chart-legend__item {
  display: flex;
  align-items: center;
  gap: 6px;
  font-size: 0.75rem;
  color: var(--text-secondary);
}

.chart-legend__dot {
  width: 8px;
  height: 8px;
  border-radius: 50%;
}

.chart-legend__dot--temp { background: #FF8A80; }
.chart-legend__dot--hum { background: var(--secondary); }
.chart-legend__dot--gas { background: var(--primary); }

.history-controls {
  display: flex;
  align-items: center;
  gap: 8px;
  flex-wrap: wrap;
  margin: -4px 0 16px;
}

.history-controls__btn {
  appearance: none;
  border: 1px solid var(--card-border);
  background: rgba(255, 255, 255, 0.055);
  color: var(--text-secondary);
  border-radius: 50px;
  padding: 7px 13px;
  font-family: var(--font);
  font-size: 0.68rem;
  font-weight: 700;
  letter-spacing: 1px;
  cursor: pointer;
  transition: color var(--transition), border-color var(--transition), background var(--transition), box-shadow var(--transition);
}

.history-controls__btn:hover,
.history-controls__btn--active {
  color: var(--primary);
  border-color: rgba(0, 229, 255, 0.3);
  background: rgba(0, 229, 255, 0.1);
  box-shadow: 0 0 18px rgba(0, 229, 255, 0.12);
}

.history-controls__btn--icon {
  display: inline-flex;
  align-items: center;
  gap: 7px;
}

.history-controls__icon {
  width: 15px;
  height: 15px;
  flex: 0 0 auto;
}

.history-controls__btn--ghost {
  color: var(--text-muted);
}

.chart-sensor-selector {
  display: flex;
  align-items: center;
  flex-wrap: wrap;
  gap: 8px;
  margin: -8px 0 16px;
}

.chart-sensor-selector__btn {
  appearance: none;
  min-width: 56px;
  padding: 7px 12px;
  border: 1px solid var(--card-border);
  border-radius: 999px;
  background: rgba(255, 255, 255, 0.05);
  color: var(--text-secondary);
  font-family: var(--font);
  font-size: 0.68rem;
  font-weight: 800;
  letter-spacing: 0.8px;
  cursor: pointer;
  transition: color var(--transition), border-color var(--transition), background var(--transition), box-shadow var(--transition);
}

.chart-sensor-selector__btn:hover,
.chart-sensor-selector__btn--active {
  color: var(--primary);
  border-color: rgba(0, 229, 255, 0.32);
  background: rgba(0, 229, 255, 0.1);
  box-shadow: 0 0 18px rgba(0, 229, 255, 0.12);
}

.date-history-panel {
  display: flex;
  align-items: center;
  gap: 8px;
  flex-wrap: wrap;
  margin: -6px 0 16px;
  padding: 10px;
  border-radius: var(--radius-sm);
  background: rgba(255, 255, 255, 0.04);
  border: 1px solid rgba(255, 255, 255, 0.07);
}

.date-history-panel--hidden {
  display: none;
}

.date-history-panel--compact {
  margin-top: -10px;
}

.date-history-panel__date {
  color: var(--primary);
  font-size: 0.8rem;
  font-variant-numeric: tabular-nums;
  letter-spacing: 0.8px;
  min-width: 150px;
  text-align: center;
}

.date-history-panel__input,
.date-history-panel__select {
  appearance: none;
  border: 1px solid var(--card-border);
  background: rgba(11, 18, 32, 0.6);
  color: var(--text-primary);
  border-radius: 999px;
  padding: 7px 12px;
  font-family: var(--font);
  font-size: 0.72rem;
  font-weight: 600;
  outline: none;
}

.date-history-panel__input:focus,
.date-history-panel__select:focus {
  border-color: rgba(0, 229, 255, 0.4);
  box-shadow: 0 0 18px rgba(0, 229, 255, 0.12);
}

.date-history-panel__toggle {
  display: inline-flex;
  align-items: center;
  gap: 7px;
  color: var(--text-secondary);
  font-size: 0.72rem;
  font-weight: 700;
  letter-spacing: 0.5px;
  cursor: pointer;
}

.date-history-panel__toggle input {
  accent-color: var(--primary);
}

.power-session-strip {
  display: grid;
  grid-template-columns: repeat(3, minmax(0, 1fr));
  gap: 8px;
  margin: -4px 0 16px;
}

.power-session-strip div {
  padding: 10px 12px;
  border-radius: var(--radius-sm);
  background: rgba(255, 255, 255, 0.045);
  border: 1px solid rgba(255, 255, 255, 0.07);
}

.power-session-strip span,
.solo-stats span,
.solo-ai span,
.sensor-modal__eyebrow {
  display: block;
  color: var(--text-muted);
  text-transform: uppercase;
  letter-spacing: 1.2px;
  font-size: 0.66rem;
  font-weight: 700;
}

.power-session-strip strong {
  display: block;
  margin-top: 4px;
  color: var(--primary);
  font-size: 0.8rem;
  font-weight: 700;
}

.chart-container {
  flex: 1;
  min-height: 280px;
  position: relative;
}

.chart-container canvas {
  width: 100% !important;
  height: 100% !important;
}

.chart-stat-strip {
  display: grid;
  grid-template-columns: repeat(4, minmax(0, 1fr));
  gap: 8px;
  margin: 12px 0 10px;
}

.chart-empty-state {
  position: absolute;
  inset: 0;
  display: none;
  align-items: center;
  justify-content: center;
  color: var(--text-secondary);
  font-size: 0.9rem;
  text-align: center;
  pointer-events: none;
}

.chart-empty-state--visible {
  display: flex;
}

.analytics-strip {
  display: grid;
  grid-template-columns: repeat(6, minmax(0, 1fr));
  gap: 8px;
  margin-top: 14px;
}

.analytics-tile {
  padding: 10px 12px;
  border-radius: var(--radius-sm);
  background: rgba(255, 255, 255, 0.045);
  border: 1px solid rgba(255, 255, 255, 0.07);
}

.analytics-tile span {
  display: block;
  color: var(--text-muted);
  text-transform: uppercase;
  letter-spacing: 1.2px;
  font-size: 0.62rem;
  font-weight: 700;
}

.analytics-tile strong {
  display: block;
  margin-top: 4px;
  color: var(--primary);
  font-size: 0.88rem;
  font-variant-numeric: tabular-nums;
}

/* ---- Side Panels ---- */
.side-panels {
  display: flex;
  flex-direction: column;
  gap: 16px;
}

/* ---- AI Panel ---- */
.ai-panel {
  padding: 28px;
  position: relative;
  overflow: hidden;
  flex: 1;
}

.glow-border {
  border-color: rgba(0, 229, 255, 0.2);
  box-shadow:
    var(--shadow-card),
    inset 0 0 30px rgba(0, 229, 255, 0.03),
    0 0 40px rgba(0, 229, 255, 0.08);
  animation: glowBorder 4s ease-in-out infinite;
}

@keyframes glowBorder {
  0%, 100% { border-color: rgba(0, 229, 255, 0.15); box-shadow: var(--shadow-card), 0 0 30px rgba(0, 229, 255, 0.06); }
  50% { border-color: rgba(0, 229, 255, 0.35); box-shadow: var(--shadow-card), 0 0 50px rgba(0, 229, 255, 0.15); }
}

.ai-panel__header {
  display: flex;
  align-items: center;
  gap: 14px;
  margin-bottom: 20px;
}

.ai-panel__icon {
  width: 48px;
  height: 48px;
  flex-shrink: 0;
  animation: logoPulse 4s ease-in-out infinite;
}

.ai-panel__icon svg {
  width: 100%;
  height: 100%;
}

.ai-panel__title {
  font-size: 1.1rem;
  font-weight: 700;
  background: linear-gradient(135deg, var(--primary), var(--secondary));
  -webkit-background-clip: text;
  -webkit-text-fill-color: transparent;
  background-clip: text;
}

.ai-panel__text {
  font-size: 1.05rem;
  font-weight: 400;
  line-height: 1.7;
  color: var(--text-primary);
  position: relative;
  z-index: 1;
}

.recommendation-level--elevated {
  color: #FFE082;
}

.recommendation-level--warning {
  color: var(--warning);
}

.recommendation-level--danger {
  color: var(--danger);
}

.ai-panel__glow {
  position: absolute;
  bottom: -40px;
  right: -40px;
  width: 200px;
  height: 200px;
  border-radius: 50%;
  background: radial-gradient(circle, rgba(0, 229, 255, 0.08) 0%, transparent 70%);
  pointer-events: none;
}

/* ---- System Panel ---- */
.system-panel {
  padding: 24px;
}

.system-grid {
  display: flex;
  flex-direction: column;
  gap: 16px;
}

.system-summary {
  margin: -4px 0 16px;
  color: var(--text-secondary);
  font-size: 0.86rem;
  line-height: 1.55;
 }

.system-item {
  display: flex;
  flex-direction: column;
  gap: 6px;
  padding: 8px;
  border: 1px solid transparent;
  border-radius: var(--radius-sm);
  transition: border-color var(--transition), background var(--transition), box-shadow var(--transition);
}

.system-item--inline {
  flex-direction: row;
  align-items: center;
  justify-content: space-between;
}

.system-item__label {
  font-size: 0.75rem;
  font-weight: 500;
  color: var(--text-muted);
  text-transform: uppercase;
  letter-spacing: 1px;
}

.system-item__value {
  font-size: 1.1rem;
  font-weight: 600;
  font-variant-numeric: tabular-nums;
  color: var(--primary);
}

.system-item__badge {
  font-size: 0.8rem;
  font-weight: 600;
  padding: 4px 12px;
  border-radius: 50px;
}

.system-item__badge--online {
  background: rgba(0, 230, 118, 0.12);
  color: var(--success);
  border: 1px solid rgba(0, 230, 118, 0.25);
}

.system-item__badge--offline {
  background: rgba(255, 82, 82, 0.12);
  color: var(--danger);
  border: 1px solid rgba(255, 82, 82, 0.25);
}

.progress-bar {
  width: 100%;
  height: 4px;
  background: rgba(255, 255, 255, 0.06);
  border-radius: 4px;
  overflow: hidden;
}

.progress-bar__fill {
  height: 100%;
  border-radius: 4px;
  transition: width 0.8s cubic-bezier(0.4, 0, 0.2, 1);
}

.progress-bar__fill--temp { background: linear-gradient(90deg, var(--secondary), #FF8A80); }
.progress-bar__fill--cpu { background: linear-gradient(90deg, var(--secondary), var(--primary)); }
.progress-bar__fill--ram { background: linear-gradient(90deg, var(--primary), var(--success)); }

/* ---- Footer ---- */
.footer {
  display: flex;
  align-items: center;
  justify-content: center;
  flex-wrap: wrap;
  gap: 24px;
  padding: 14px 24px;
  margin-top: auto;
}

.footer__item {
  display: flex;
  align-items: center;
  gap: 8px;
  font-size: 0.75rem;
  font-weight: 500;
  color: var(--text-secondary);
}

.footer__dot {
  width: 6px;
  height: 6px;
  border-radius: 50%;
}

.footer__dot--online {
  background: var(--success);
  box-shadow: 0 0 6px var(--success);
  animation: dotPulse 2s ease-in-out infinite;
}

.footer__version {
  font-size: 0.75rem;
  font-weight: 600;
  color: var(--text-muted);
  letter-spacing: 1px;
  margin-left: auto;
}

/* ---- Animations ---- */
.fade-in {
  opacity: 0;
  transform: translateY(20px);
  animation: fadeInUp 0.7s cubic-bezier(0.4, 0, 0.2, 1) forwards;
}

.fade-in--delay-1 { animation-delay: 0.1s; }
.fade-in--delay-2 { animation-delay: 0.15s; }
.fade-in--delay-3 { animation-delay: 0.2s; }
.fade-in--delay-4 { animation-delay: 0.25s; }
.fade-in--delay-5 { animation-delay: 0.3s; }
.fade-in--delay-6 { animation-delay: 0.35s; }
.fade-in--delay-7 { animation-delay: 0.4s; }
.fade-in--delay-8 { animation-delay: 0.45s; }
.fade-in--delay-9 { animation-delay: 0.5s; }

/* ---- V6 Solo Sensor Analytics ---- */
.sensor-card {
  cursor: pointer;
}

.system-item[data-system-metric] {
  cursor: pointer;
}

.system-item--chart-focus {
  border-color: rgba(0, 229, 255, 0.28);
  background: rgba(0, 229, 255, 0.075);
  box-shadow: 0 0 16px rgba(0, 229, 255, 0.1);
}

.system-item--warning {
  border-color: rgba(255, 213, 79, 0.35);
  background: rgba(255, 213, 79, 0.08);
}

.system-item--danger-level,
.system-item--danger {
  border-color: rgba(255, 82, 82, 0.4);
  background: rgba(255, 82, 82, 0.08);
}

.system-item--danger {
  width: 100%;
  text-align: left;
  font-family: var(--font);
  cursor: pointer;
}

.system-item--danger .system-item__value,
.system-item--danger-level .system-item__value {
  color: var(--danger);
}

.sensor-modal {
  position: fixed;
  inset: 0;
  z-index: 18;
  display: flex;
  align-items: center;
  justify-content: center;
  padding: 24px;
  background: rgba(11, 18, 32, 0.66);
  backdrop-filter: blur(14px);
  -webkit-backdrop-filter: blur(14px);
  opacity: 1;
  pointer-events: auto;
  transition: opacity 0.35s ease, visibility 0.35s ease;
}

.sensor-modal--hidden {
  opacity: 0;
  visibility: hidden;
  pointer-events: none;
}

.sensor-modal__card {
  width: min(860px, 100%);
  padding: 24px;
  position: relative;
}

.sensor-modal__close {
  position: absolute;
  top: 14px;
  right: 14px;
  width: 36px;
  height: 36px;
  border-radius: 50%;
  border: 1px solid var(--card-border);
  background: rgba(255, 255, 255, 0.06);
  color: var(--text-primary);
  font-size: 1.4rem;
  line-height: 1;
  cursor: pointer;
}

.sensor-modal__header {
  display: flex;
  align-items: center;
  justify-content: space-between;
  gap: 16px;
  padding-right: 44px;
  margin-bottom: 18px;
}

.sensor-modal__header h2 {
  font-size: 1.35rem;
  font-weight: 800;
  letter-spacing: 0.8px;
}

.offline-overlay {
  position: fixed;
  inset: 0;
  z-index: 30;
  display: flex;
  align-items: center;
  justify-content: center;
  padding: 24px;
  background: rgba(5, 12, 23, 0.58);
  backdrop-filter: blur(14px);
  -webkit-backdrop-filter: blur(14px);
  opacity: 1;
  visibility: visible;
  pointer-events: auto;
  transition: opacity 0.3s ease, visibility 0.3s ease;
}

.offline-overlay--hidden {
  opacity: 0;
  visibility: hidden;
  pointer-events: none;
}

.offline-card {
  width: min(560px, 100%);
  padding: 28px;
  text-align: left;
}

.offline-card__eyebrow {
  display: block;
  color: var(--text-muted);
  font-size: 0.72rem;
  font-weight: 800;
  letter-spacing: 1.6px;
  text-transform: uppercase;
  margin-bottom: 10px;
}

.offline-card h2 {
  color: var(--text-primary);
  font-size: clamp(1.6rem, 4vw, 2.1rem);
  line-height: 1.12;
  margin-bottom: 12px;
}

.offline-card p {
  color: var(--text-secondary);
  font-size: 0.98rem;
  line-height: 1.65;
}

.offline-card strong {
  color: var(--primary);
}

.offline-card__actions {
  display: flex;
  flex-wrap: wrap;
  gap: 10px;
  margin-top: 18px;
}

.offline-card__actions span {
  padding: 8px 12px;
  border: 1px solid rgba(0, 229, 255, 0.18);
  border-radius: 999px;
  background: rgba(0, 229, 255, 0.08);
  color: var(--primary);
  font-size: 0.78rem;
  font-weight: 700;
}

.export-modal__card {
  max-width: 560px;
}

.export-modal__grid {
  display: grid;
  grid-template-columns: repeat(2, minmax(0, 1fr));
  gap: 12px;
  margin-top: 18px;
}

.export-modal__grid label {
  display: flex;
  flex-direction: column;
  gap: 7px;
}

.export-modal__grid span,
.export-modal__status {
  color: var(--text-muted);
  font-size: 0.72rem;
  font-weight: 700;
  letter-spacing: 1px;
  text-transform: uppercase;
}

.export-modal__actions {
  display: flex;
  align-items: center;
  gap: 12px;
  margin-top: 18px;
}

.export-modal__status {
  color: var(--primary);
  text-transform: none;
  letter-spacing: 0;
}

.solo-stats {
  display: grid;
  grid-template-columns: repeat(4, minmax(0, 1fr));
  gap: 10px;
  margin-bottom: 16px;
}

.solo-stats div,
.solo-ai {
  padding: 14px;
  border-radius: var(--radius-sm);
  background: rgba(255, 255, 255, 0.045);
  border: 1px solid rgba(255, 255, 255, 0.07);
}

.solo-stats strong {
  display: block;
  margin-top: 6px;
  color: var(--primary);
  font-size: 1.2rem;
  font-variant-numeric: tabular-nums;
}

.solo-chart-container {
  height: 280px;
  position: relative;
  margin-bottom: 16px;
}

.solo-chart-container canvas {
  width: 100% !important;
  height: 100% !important;
}

.solo-ai p {
  margin-top: 6px;
  color: var(--text-primary);
  line-height: 1.6;
}

/* ---- V6 Boot + Calibration Overlays ---- */
.boot-screen,
.calibration-overlay {
  position: fixed;
  inset: 0;
  z-index: 20;
  display: flex;
  align-items: center;
  justify-content: center;
  padding: 24px;
  background: rgba(11, 18, 32, 0.82);
  backdrop-filter: blur(18px);
  -webkit-backdrop-filter: blur(18px);
  opacity: 1;
  pointer-events: auto;
  transition: opacity 0.8s ease, visibility 0.8s ease;
}

.boot-screen--hidden,
.calibration-overlay--hidden {
  opacity: 0;
  pointer-events: none;
  visibility: hidden;
}

.calibration-overlay {
  z-index: 12;
  align-items: flex-end;
  justify-content: flex-end;
  background: transparent;
  backdrop-filter: none;
  -webkit-backdrop-filter: none;
}

.boot-card,
.calibration-card {
  width: min(720px, 100%);
  padding: 30px;
  position: relative;
  overflow: hidden;
}

.boot-card::before,
.calibration-card::before {
  content: '';
  position: absolute;
  inset: 0;
  background: linear-gradient(120deg, transparent, rgba(0, 229, 255, 0.08), transparent);
  transform: translateX(-100%);
  animation: bootSheen 3s ease-in-out infinite;
  pointer-events: none;
}

@keyframes bootSheen {
  0% { transform: translateX(-100%); }
  55%, 100% { transform: translateX(100%); }
}

.boot-card__brand {
  display: flex;
  align-items: center;
  gap: 18px;
  margin-bottom: 24px;
}

.boot-card__mark {
  width: 58px;
  height: 58px;
  border-radius: 50%;
  border: 1px solid rgba(0, 229, 255, 0.45);
  box-shadow: inset 0 0 24px rgba(0, 229, 255, 0.14), 0 0 28px rgba(0, 229, 255, 0.2);
  position: relative;
  animation: logoPulse 3s ease-in-out infinite;
}

.boot-card__mark::after {
  content: '';
  position: absolute;
  inset: 18px;
  border-radius: 50%;
  background: var(--primary);
  box-shadow: 0 0 18px var(--primary);
}

.boot-card h1,
.calibration-card h2 {
  font-size: 1.65rem;
  font-weight: 800;
  letter-spacing: 1.5px;
}

.boot-card p,
.calibration-card p {
  color: var(--text-secondary);
  font-size: 0.9rem;
}

.boot-card__time {
  display: flex;
  flex-direction: column;
  gap: 2px;
  margin-bottom: 22px;
}

.boot-card__time span {
  font-size: 2.2rem;
  font-weight: 800;
  font-variant-numeric: tabular-nums;
  background: linear-gradient(135deg, var(--primary), #FFFFFF);
  -webkit-background-clip: text;
  -webkit-text-fill-color: transparent;
  background-clip: text;
}

.boot-card__time small,
.boot-progress__meta span,
.calibration-card__meta span,
.calibration-card__eyebrow {
  color: var(--text-muted);
  text-transform: uppercase;
  letter-spacing: 1.4px;
  font-size: 0.68rem;
  font-weight: 700;
}

.boot-progress,
.calibration-card__content {
  display: grid;
  grid-template-columns: auto 1fr;
  gap: 24px;
  align-items: center;
}

.progress-ring {
  --progress: 0;
  width: 132px;
  height: 132px;
  border-radius: 50%;
  display: grid;
  place-items: center;
  background: conic-gradient(var(--primary) calc(var(--progress) * 1%), rgba(255, 255, 255, 0.08) 0);
  position: relative;
  box-shadow: 0 0 30px rgba(0, 229, 255, 0.12);
}

.progress-ring::after {
  content: '';
  position: absolute;
  inset: 10px;
  border-radius: 50%;
  background: rgba(11, 18, 32, 0.92);
  border: 1px solid rgba(255, 255, 255, 0.08);
}

.progress-ring span {
  position: relative;
  z-index: 1;
  font-weight: 800;
  font-variant-numeric: tabular-nums;
}

.progress-ring--calibration {
  width: 104px;
  height: 104px;
}

.boot-progress__meta,
.calibration-card__meta {
  display: grid;
  grid-template-columns: repeat(2, minmax(0, 1fr));
  gap: 14px;
}

.boot-progress__meta div,
.calibration-card__meta div {
  padding: 12px;
  border-radius: var(--radius-sm);
  background: rgba(255, 255, 255, 0.045);
  border: 1px solid rgba(255, 255, 255, 0.07);
}

.boot-progress__meta strong,
.calibration-card__meta strong {
  display: block;
  margin-top: 4px;
  color: var(--primary);
  font-size: 1rem;
  font-variant-numeric: tabular-nums;
}

.boot-bar {
  width: 100%;
  height: 6px;
  background: rgba(255, 255, 255, 0.07);
  border-radius: 999px;
  overflow: hidden;
  margin-top: 22px;
}

.boot-bar__fill {
  height: 100%;
  width: 0%;
  border-radius: inherit;
  background: linear-gradient(90deg, var(--secondary), var(--primary), var(--success));
  transition: width 0.6s ease;
}

.boot-timeline {
  list-style: none;
  display: grid;
  grid-template-columns: repeat(3, minmax(0, 1fr));
  gap: 10px;
  margin-top: 22px;
}

.boot-timeline li {
  color: var(--text-secondary);
  font-size: 0.76rem;
  padding: 8px 10px;
  border-radius: 999px;
  border: 1px solid rgba(255, 255, 255, 0.07);
  background: rgba(255, 255, 255, 0.04);
}

.boot-timeline li.is-active,
.boot-timeline li.is-done {
  color: var(--primary);
  border-color: rgba(0, 229, 255, 0.25);
  background: rgba(0, 229, 255, 0.08);
}

.calibration-card {
  width: min(520px, 100%);
}

@keyframes fadeInUp {
  to {
    opacity: 1;
    transform: translateY(0);
  }
}

/* ---- Responsive: Raspberry Pi 7" (800x480) ---- */
@media (max-width: 1024px) {
  .sensor-grid {
    grid-template-columns: repeat(4, minmax(0, 1fr));
    gap: 10px;
  }

  .main-panels {
    grid-template-columns: 1fr;
  }

  .side-panels {
    flex-direction: row;
  }

  .ai-panel,
  .system-panel {
    flex: 1;
  }

  .header__title {
    font-size: 1.4rem;
  }

  .header__time {
    font-size: 1.5rem;
  }

  .sensor-card__value {
    font-size: 1.5rem;
  }

  .sensor-card {
    padding: 14px;
  }

  .chart-container {
    min-height: 220px;
  }
}

@media (max-width: 800px) {
  .app {
    padding: 10px;
    gap: 10px;
  }

  .header {
    flex-direction: column;
    text-align: center;
    padding: 16px;
  }

  .header__brand {
    flex-direction: column;
  }

  .header__clock {
    text-align: center;
  }

  .status-bar {
    padding: 12px;
  }

  .status-bar__divider {
    display: none;
  }

  .status-bar__item {
    min-width: auto;
    flex: 1;
  }

  .sensor-grid {
    grid-template-columns: repeat(2, 1fr);
    gap: 8px;
  }

  .side-panels {
    flex-direction: column;
  }

  .footer__version {
    margin-left: 0;
    width: 100%;
    text-align: center;
  }

  .footer {
    gap: 12px;
  }

  .boot-progress,
  .calibration-card__content {
    grid-template-columns: 1fr;
  }

  .boot-progress__meta,
  .calibration-card__meta,
  .boot-timeline {
    grid-template-columns: 1fr;
  }

  .power-session-strip,
  .solo-stats,
  .analytics-strip {
    grid-template-columns: 1fr;
  }

  .date-history-panel {
    align-items: stretch;
  }

  .date-history-panel__date,
  .date-history-panel__input,
  .date-history-panel__select,
  .date-history-panel .history-controls__btn {
    flex: 1 1 100%;
  }

  .sensor-modal {
    align-items: flex-start;
    overflow-y: auto;
  }

  .sensor-modal__header {
    align-items: flex-start;
    flex-direction: column;
  }

  .progress-ring {
    margin: 0 auto;
  }
}

@media (max-width: 480px) {
  html {
    font-size: 14px;
  }

  .header__title {
    font-size: 1.2rem;
  }

  .header__subtitle {
    font-size: 0.7rem;
  }

  .sensor-card__value {
    font-size: 1.3rem;
  }

  .sensor-card__icon {
    width: 32px;
    height: 32px;
  }

  .sensor-card__icon svg {
    width: 18px;
    height: 18px;
  }

  .chart-container {
    min-height: 180px;
  }

  .ai-panel__text {
    font-size: 0.95rem;
  }
}

/* Touch-friendly for Raspberry Pi touchscreen */
@media (pointer: coarse) {
  .sensor-card {
    min-height: 120px;
  }

  .sensor-card:active {
    transform: scale(0.98);
  }
}

/* V7 classroom theme: bright, friendly, and readable on shared displays. */
:root {
  color-scheme: light;
  --bg-primary: #f2f9fc;
  --card-bg: #ffffff;
  --card-border: #cfe3ec;
  --card-hover: #f7fcfe;
  --primary: #079db7;
  --secondary: #377ee8;
  --success: #15966b;
  --warning: #d99512;
  --danger: #dc4c5d;
  --text-primary: #17364a;
  --text-secondary: #5d7587;
  --text-muted: #8295a3;
  --shadow-glow: 0 0 0 3px rgba(7, 157, 183, 0.09);
  --shadow-card: 0 9px 25px rgba(47, 91, 112, 0.09);
}

body {
  background: #f2f9fc;
  color: var(--text-primary);
}

.bg-gradient {
  display: block;
  background: #f2f9fc;
  animation: none;
}

.bg-grid {
  background-image:
    linear-gradient(rgba(35, 139, 167, 0.045) 1px, transparent 1px),
    linear-gradient(90deg, rgba(35, 139, 167, 0.045) 1px, transparent 1px);
  background-size: 36px 36px;
  mask-image: none;
  opacity: 1;
}

.bg-glow { display: none; }

.glass-panel {
  background: rgba(255, 255, 255, 0.96);
  border: 1px solid var(--card-border);
  border-radius: 8px;
  box-shadow: var(--shadow-card);
  backdrop-filter: blur(12px);
  -webkit-backdrop-filter: blur(12px);
}

.glass-panel::before,
.glass-panel::after { display: none; }

.header {
  background: #ffffff;
  border-left: 5px solid #17b890;
}

.header__logo {
  background: #e4f8fb;
  border-radius: 8px;
}

.header__title { color: #17364a; }
.header__subtitle,
.header__date { color: #688092; }
.header__time { color: #087f98; text-shadow: none; }

.status-bar {
  background: #ffffff;
  border-top: 4px solid #87d8e5;
}

.status-bar__divider { background: #dceaf0; }
.status-bar__label { color: #708796; }
.status-bar__value { color: #087f98; }

.status-badge {
  background: #eef8f5;
  box-shadow: none;
}

.status-badge--safe { color: #087c59; border-color: #83d8bd; background: #e7f8f1; }
.status-badge--elevated { color: #9a6500; border-color: #f1cd79; background: #fff7dd; }
.status-badge--warning { color: #a95315; border-color: #f3b685; background: #fff0e6; }
.status-badge--danger { color: #b92d40; border-color: #ef9ba6; background: #fff0f2; }
.status-badge--calibrating { color: #087f98; border-color: #8dd7e4; background: #e9f9fc; }

.connection-dot__label { color: #4d6778; }
.connection-dot--online .connection-dot__label { color: #087c59; }

.sensor-card {
  background: #ffffff;
  border: 1px solid #cfe3ec;
  border-top: 4px solid #19abc3;
  border-radius: 8px;
  box-shadow: 0 7px 19px rgba(47, 91, 112, 0.07);
}

.sensor-card:nth-child(4n + 1) { border-top-color: #f07c74; background: #fffafa; }
.sensor-card:nth-child(4n + 2) { border-top-color: #4e91e8; background: #f8fbff; }
.sensor-card:nth-child(4n + 3) { border-top-color: #18b896; background: #f7fdfa; }
.sensor-card:nth-child(4n + 4) { border-top-color: #efb43c; background: #fffcf5; }

.sensor-card:hover,
.sensor-card:focus-visible {
  border-color: #68bfd0;
  box-shadow: 0 12px 26px rgba(38, 113, 139, 0.14);
  transform: translateY(-3px);
  outline: none;
}

.sensor-card__icon {
  background: #e6f7fa;
  border: 1px solid #c7e9ef;
  border-radius: 8px;
  color: #087f98;
}

.sensor-card__icon--temp { color: #d85252; background: #fff0ee; border-color: #f5d0cc; }
.sensor-card__icon--hum { color: #377ee8; background: #edf5ff; border-color: #cfe0f7; }
.sensor-card__icon--gas { color: #087f98; background: #e7f8fb; border-color: #c7e9ef; }
.sensor-card__name { color: #577184; }
.sensor-card__value {
  color: #17364a;
  background: none;
  -webkit-text-fill-color: #17364a;
  text-shadow: none;
}
.sensor-card__unit { color: #7890a0; }
.sensor-card__status-text { color: #5d7587; }

.main-grid { align-items: stretch; }
.chart-panel,
.ai-panel,
.system-panel { background: #ffffff; border-radius: 8px; }

.chart-panel__title,
.ai-panel__title,
.system-panel__title { color: #17364a; }

.chart-panel__subtitle,
.ai-panel__text,
.system-summary { color: #5d7587; }

.history-controls__btn,
.chart-sensor-selector__btn,
.date-history-panel__input,
.date-history-panel__select {
  color: #38576b;
  background: #f4f9fb;
  border-color: #c9dfe8;
  border-radius: 7px;
}

.history-controls__btn:hover,
.chart-sensor-selector__btn:hover {
  color: #087f98;
  background: #e7f7fa;
  border-color: #74c8d7;
}

.history-controls__btn--active,
.chart-sensor-selector__btn--active {
  color: #ffffff;
  background: #079db7;
  border-color: #079db7;
  box-shadow: 0 5px 12px rgba(7, 157, 183, 0.18);
}

.chart-container,
.solo-chart-container {
  background: #fbfdfe;
  border: 1px solid #d7e7ee;
  border-radius: 8px;
}

.chart-empty-state { color: #6a8292; }
.chart-metric-strip,
.analytics-strip,
.power-session-strip { border-color: #dce9ef; }

.chart-metric-strip > div,
.analytics-tile,
.power-session-strip > div,
.solo-stats > div,
.system-item {
  background: #f7fbfc;
  border-color: #d7e7ee;
  border-radius: 7px;
}

.analytics-tile strong,
.power-session-strip strong,
.solo-stats strong,
.system-item__value { color: #087f98; }

.ai-panel {
  border-left: 5px solid #17b890;
  background: #f5fcf9;
}

.ai-panel__icon { color: #0d8d68; background: #e2f6ee; border-radius: 8px; }
.ai-panel__glow { display: none; }

.system-item:hover {
  background: #eef8fb;
  border-color: #a8d6e0;
  transform: translateY(-1px);
  box-shadow: none;
}

.progress-bar { background: #deebf0; }
.progress-bar__fill { box-shadow: none; }

.footer {
  color: #5d7587;
  background: #ffffff;
  border-top: 4px solid #cfeaf0;
}

.footer__version { color: #7d92a1; }

.sensor-modal { background: rgba(35, 61, 77, 0.42); }
.sensor-modal__card {
  background: #ffffff;
  border: 1px solid #c5dce6;
  border-top: 5px solid #079db7;
  border-radius: 8px;
  box-shadow: 0 24px 70px rgba(29, 64, 83, 0.22);
}

.sensor-modal__close {
  color: #557082;
  background: #edf5f8;
  border-color: #c9dfe8;
}

.solo-ai { color: #466476; background: #edf9f5; border-color: #bfe6d8; }
.solo-ai span { color: #0d8d68; }

.boot-screen,
.calibration-overlay {
  align-items: center;
  justify-content: center;
  background: rgba(232, 244, 249, 0.94);
  backdrop-filter: blur(12px);
  -webkit-backdrop-filter: blur(12px);
}

.boot-card,
.calibration-card {
  color: #17364a;
  background: #ffffff;
  border: 1px solid #bed8e3;
  border-top: 6px solid #17b890;
  border-radius: 8px;
  box-shadow: 0 24px 70px rgba(29, 64, 83, 0.15);
}

.boot-card__mark { background: #10abc3; box-shadow: 0 0 0 9px #e2f7fa; }
.boot-card h1,
.calibration-card h2 { color: #17364a; }
.boot-card p,
.calibration-card p { color: #647d8e; }
.boot-card__time span,
.calibration-card__meta strong { color: #087f98; }
.boot-card__time span { background: none; -webkit-text-fill-color: #087f98; }
.boot-card__time small,
.boot-progress__meta span,
.calibration-card__meta span,
.calibration-card__eyebrow { color: #728897; }

.boot-progress__meta div,
.calibration-card__meta div,
.boot-timeline li {
  background: #f5fafc;
  border-color: #d6e7ee;
}

.progress-ring { background: conic-gradient(#079db7 calc(var(--progress) * 1%), #dfedf2 0); }
.progress-ring::after { background: #ffffff; border-color: #d8e8ee; }
.progress-ring span { color: #17364a; }
.boot-bar { background: #dcebef; }
.boot-bar__fill { background: #17b890; box-shadow: none; }

.offline-overlay { background: rgba(232, 244, 249, 0.96); }
.offline-card { color: #17364a; background: #ffffff; border-color: #efb5bd; }
.offline-card p { color: #61798a; }

@media (max-width: 760px) {
  .app { padding: 10px; gap: 10px; }
  .header { border-left-width: 4px; }
  .sensor-card { padding: 18px; }
  .glass-panel { box-shadow: 0 5px 15px rgba(47, 91, 112, 0.07); }
}
